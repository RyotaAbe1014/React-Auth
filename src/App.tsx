import { Router } from './router/Router';
import React, { FC, memo, ReactNode, useEffect, useState } from "react";
const App: FC = () => {

  return (
    <div className="App">
      <Router />
    </div>
  );
}

export default App;
